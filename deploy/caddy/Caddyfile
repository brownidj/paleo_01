{
    admin off
}

http://{$SERVER_HOST:localhost} {
    redir https://{$SERVER_HOST:localhost}{uri}
}

{$SERVER_HOST:localhost} {
    tls internal
    # LAN-only access for local-staging mode.
    @not_lan not remote_ip private_ranges
    respond @not_lan 403
    reverse_proxy api:8000
}
