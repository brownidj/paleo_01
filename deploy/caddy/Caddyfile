{
    admin off
    auto_https off
}

http://{$SERVER_HOST:localhost} {
    # Allow RFC1918 LAN and Tailscale clients only.
    @allowed remote_ip private_ranges 100.64.0.0/10
    handle @allowed {
        reverse_proxy api:8000
    }
    respond 403
}
